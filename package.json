{
  "name": "elenkos-systems-pages",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/verify-built-site.mjs",
    "check": "astro check",
    "test": "node --test tests/*.test.mjs",
    "validate": "pnpm check && pnpm test && pnpm build"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "@astrojs/sitemap": "3.7.3",
    "astro": "7.2.8",
    "typescript": "6.0.3"
  }
}
